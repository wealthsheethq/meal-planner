{
  "name": "meal-planner",
  "private": true,
  "type": "module",
  "description": "Shared meal planning + grocery app. Static site, no build step.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:ui": "node tests/ui-check.mjs"
  }
}
